<% # app/views/records/edit_fields/_funding_source.html.erb %>

<%# auto-complete field below %>
<%=
  f.input :funding_source,
  as: :multi_value,
  input_html: {
    class: 'form-control',
    data: { 'autocomplete-url' => "/authorities/search/local/vdc_funding_sources", 'autocomplete' => :funding_source }
  } ,
  required: f.object.required?(key) %>

<%# select option commented out below %>
<%# f.input :funding_source, :label => "Funder", required: f.object.required?(key),
    as: :select,
    collection: VdcFundingSourcesService.select_all_options,
    include_blank: false,
    #item_helper: method(:include_current_value),
    input_html: { class: 'form-control', multiple: false }
%>

<%# multi-value text field commented out below%>
<%# f.input :funding_source, :label => "Funder", required: f.object.required?(key),
  as: :select,
  collecton: VdcFundingSourcesService.select_all_options,
  include_blank: false,
  as: :multi_value,
  input_html: { class: 'form-control', multiple: true }
%>
